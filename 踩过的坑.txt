//空




起名字不规范，导致不知道表示的什么东西。比如不对应

避免没用的临时变量


总是输出删除的最后空格字符，导致以为程序错误，应该直接输出“删除那行程序语句”

输出x的时候，不应该在for 循环内输出x，应该在for循环外输出x,由于数据是一个241,2594,904

复制代码忘改名字

git 的使用：
首先在本地创建ssh key：   ssh-keygen -t rsa -C "your_email@youremail.com"
了验证是否成功，在Git bash下输入：   ssh -T git@github.com

///////////////////////////////////twoday/////////////////////////////////////////
x_sum = 0  #这句应该放到第一个for循环中，在第2个for循环中会被覆盖掉
元组如何删掉最后一个元素 pop及没有这种方法
删除单独一个元组不可能
